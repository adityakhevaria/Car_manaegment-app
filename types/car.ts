// app/types/car.ts (or directly in the component file)
export interface Car {
  title: string
  description: string
  carType: string
  company: string
  dealer: string
  images: string[] // Array of image URLs
}
